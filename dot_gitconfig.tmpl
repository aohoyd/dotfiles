[user]
	email = {{ .email }}
	name = Aleksei Olshanskii
[gitget]
    root = {{ .chezmoi.homedir }}/code
    skip-host = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
